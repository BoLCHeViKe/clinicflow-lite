# ── IP del host (solo cambiar esto si cambia la IP de la VM) ────
APP_HOST_IP=#

# ── Backend ──────────────────────────────────────────────────────
PORT=#
NODE_ENV=#
DB_HOST=#
DB_USER=#
DB_PASSWORD=#
DB_NAME=#
DB_PORT=#
JWT_SECRET=#
JWT_EXPIRES_IN=#

# ── MySQL (imagen oficial Docker) ─────────────────────────────────
MYSQL_ROOT_PASSWORD=#
MYSQL_DATABASE=#
MYSQL_USER=#
MYSQL_PASSWORD=#
