# Servicio: db) ────
MYSQL_ROOT_PASSWORD=# (La contraseña maestra)

MYSQL_DATABASE=# (El nombre de la base de datos que se creará)

MYSQL_USER=# (El usuario con permisos)

MYSQL_PASSWORD=# (La contraseña de dicho usuario)

# ── Servicio: backend Service──────────────────────────────────────────────────────
DB_HOST=# Debe ser db (el nombre del servicio en tu compose).

DB_PORT=# 3306.

DB_USER=# (El mismo que pusiste en el grupo DB).

DB_PASSWORD=# (El mismo que pusiste en el grupo DB).

DB_NAME=# (El mismo que pusiste en el grupo DB).

PORT=# 3001 (o el que use tu servidor Node).

NODE_ENV=# production.

JWT_SECRET=# Una cadena larga y aleatoria.

JWT_EXPIRES_IN=# 24h (o tu preferencia 7d).

FRONTEND_URL=# Aquí pegarás la URL que te dé Railway para el frontend (ej. https://tu-web.up.railway.app).



# ── Servicio: frontend Service──────────────────────────────────────────────────────
BACKEND_URL=# Aquí pegarás la URL que te dé Railway para el backend (ej. https://tu-api.up.railway.app).

APP_HOST_IP=# Generalmente se usa para configurar el dominio o la IP; en Railway puedes poner la URL del backend aquí también.

PORT=# 4200 (Recuerda añadir esta variable para que Railway sepa que debe abrir ese puerto).
