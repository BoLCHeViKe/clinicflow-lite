# ── IP del host (solo cambiar esto si cambia la IP de la VM) ────
APP_HOST_IP=#192.168.11.193

# ── Backend ──────────────────────────────────────────────────────
PORT=#3001
NODE_ENV=#development
DB_HOST=#db
DB_USER=#
DB_PASSWORD=#
DB_NAME=#
DB_PORT=#3306
JWT_SECRET=#
JWT_EXPIRES_IN=#7d

# ── MySQL (imagen oficial Docker) ─────────────────────────────────
MYSQL_ROOT_PASSWORD=#
MYSQL_DATABASE=#
MYSQL_USER=#
MYSQL_PASSWORD=#
